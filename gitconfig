[user]
	name = Tyler Cipriani
	email = tcipriani@wikimedia.org
[alias]
	lool = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
	lol = log --graph --date=short --oneline --abbrev-commit --all --graph --decorate
	ll = log --graph --date=short --oneline --abbrev-commit --all --graph --decorate -10
	up = "!git remote update -p; git merge --ff-only @{u}"
	last = log -1 HEAD
	ack = grep --break --heading --line-number
	co = checkout
	br = branch
	ci = commit
	st = status
	unstage = reset HEAD --
	edit = !vim $(git ls-files -m) -p
	whatadded = log --diff-filter=A
[color]
	ui = true
[push]
	default = matching
[status]
	submoduleSummary = true
[diff]
	submodule = log
[grep]
	extendRegexp = true
